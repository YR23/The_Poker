chart/40bb — 40bb stack charts (simplified lineup, no UTG).

Structure matches the navigation grid: MP, LJ, HJ, CO, BU, SB, BB only.
RFI filenames include chart labels (e.g. MP RFI 19.4%.json); weights are
seeded from chart/100bb until you replace with 40bb solver output.

vs BB All-in.json (SB, BU) are placeholders — fill from your solver.
